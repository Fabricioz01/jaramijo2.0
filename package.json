{
  "name": "municipio-jaramijo",
  "version": "1.0.0",
  "description": "Sistema web de gestión de tareas basado en jerarquía organizativa",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run start",
    "dev:custom": "npm --prefix frontend run dev-server",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:custom\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "start": "npm run build && npm run dev:all"
  },
  "keywords": [
    "gestión",
    "tareas",
    "municipio",
    "angular",
    "express"
  ],
  "author": "Municipio Jaramijó",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
